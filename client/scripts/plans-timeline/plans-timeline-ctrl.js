(function () {
    'use strict';

    angular.module('app')
        .controller('plansTimeLineCtrl', ['$scope', '$routeParams', '$http', 'filterFilter', 'AuthService', ctrlFunction]);

        function ctrlFunction($scope, $routeParams, $http, filterFilter, AuthService) {

            function init() {

            }
            init();


        }
}());
